import { Routes } from '@angular/router';
import { IngredientListComponent } from './ingredient-list.component';
export const INGREDIENT_ROUTES: Routes = [{ path: '', component: IngredientListComponent }];
